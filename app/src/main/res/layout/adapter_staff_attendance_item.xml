<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header Row: Staff Name and Attendance Type -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Staff Name and Info -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/staffNameTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Staff Name"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/black" />

                <TextView
                    android:id="@+id/employeeIdTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="ID: EMP001"
                    android:textSize="12sp"
                    android:textColor="@color/gray"
                    android:layout_marginTop="4dp" />
            </LinearLayout>

            <!-- Attendance Type Badge -->
            <androidx.cardview.widget.CardView
                android:id="@+id/attendanceTypeCard"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                android:layout_marginStart="8dp">

                <TextView
                    android:id="@+id/attendanceTypeTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Present"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:paddingTop="6dp"
                    android:paddingBottom="6dp" />
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/light_gray"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp" />

        <!-- Staff Details Grid -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Left Column -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/roleTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Teacher"
                    android:textSize="14sp"
                    android:textColor="@color/gray"
                    android:drawableStart="@drawable/ic_fa_user"
                    android:drawablePadding="8dp"
                    android:drawableTint="@color/gray"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:id="@+id/departmentTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Mathematics"
                    android:textSize="14sp"
                    android:textColor="@color/gray"
                    android:drawableStart="@drawable/ic_fa_building"
                    android:drawablePadding="8dp"
                    android:drawableTint="@color/gray" />
            </LinearLayout>

            <!-- Right Column -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/designationTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Senior Teacher"
                    android:textSize="14sp"
                    android:textColor="@color/gray"
                    android:drawableStart="@drawable/ic_fa_id_card"
                    android:drawablePadding="8dp"
                    android:drawableTint="@color/gray"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:id="@+id/dateTv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="07 Oct 2025"
                    android:textSize="14sp"
                    android:textColor="@color/gray"
                    android:drawableStart="@drawable/ic_fa_calendar"
                    android:drawablePadding="8dp"
                    android:drawableTint="@color/gray" />
            </LinearLayout>
        </LinearLayout>

        <!-- Remark -->
        <TextView
            android:id="@+id/remarkTv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Remark: Medical leave"
            android:textSize="13sp"
            android:textColor="@color/gray"
            android:textStyle="italic"
            android:layout_marginTop="12dp"
            android:background="@color/light_gray"
            android:padding="8dp"
            android:visibility="gone" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
